CREATE TABLE PERSON(
ID SERIAL PRIMARY KEY,
NAME VARCHAR(20),
AGE INT
);

CREATE TABLE LICENSE(
ID SERIAL PRIMARY KEY,
TYPE VARCHAR(20),
VALID_FROM DATE,
VALID_TO DATE,
PERSON_ID INT,
FOREIGN KEY (PERSON_ID)
REFERENCES PERSON(ID)
);


SELECT * FROM PERSON

SELECT * FROM LICENSE